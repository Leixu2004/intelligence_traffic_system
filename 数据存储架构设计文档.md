# 智慧交通系统 - 数据存储架构设计文档

## 1. 存储架构概述
本项目采用 **“端-边-云”** 协同的数据存储架构，以解决高并发交通车流数据的写入、存储及离线分析需求。
- **存储层 (TimescaleDB)**：基于 PostgreSQL 的时序数据库扩展，通过 Hypertable 实现海量车流记录的按时自动分区与压缩。
- **分析层 (DuckDB + Pandas)**：在边缘端/单机环境下利用 DuckDB 的内存级列式计算能力，实现免安装的极速离线报表导出。

## 2. 数据处理流水线
[YOLOv8/OCR 视觉推理] ➔ [Python psycopg2 批量组装 (连接池+重试)] ➔ [TimescaleDB 连续聚合与分区存储] ➔ [DuckDB 离线数据分析导出 CSV/Excel]

## 3. 表结构设计 (ER Schema)
核心交通数据表命名为 	raffic_data，完整遵照时序业务规范进行字段设计：

| 字段名 | 数据类型 | 约束/说明 |
| :--- | :--- | :--- |
| 	ime | TIMESTAMPTZ | PK, 检测时间，作为 Hypertable 的**分区键** |
| camera_id | TEXT | PK, 摄像头编号 (如 CAM-01) |
| plate | TEXT | 车牌号码 (由 PaddleOCR 识别得出) |
| ehicle_type | TEXT | 车型 (car, truck, bus 等，YOLO 分类) |
| confidence | REAL | AI 识别置信度 (0.0 ~ 1.0) |
| box_x1 | INT | 目标边界框 左上 X 坐标 |
| box_y1 | INT | 目标边界框 左上 Y 坐标 |
| box_x2 | INT | 目标边界框 右下 X 坐标 |
| box_y2 | INT | 目标边界框 右下 Y 坐标 |

## 4. 索引、分区与性能优化策略
1. **自动分区 (Hypertable)**：以 	ime 列作为主干，设置 chunk_time_interval 为 1 hour。数据在物理层被自动切分为多个 Chunk，极大提升时间范围查询速度。
2. **数据压缩 (Compression)**：启用原生压缩特性 	imescaledb.compress，设置 segmentby = 'camera_id'，配置策略自动压缩 7 天前的冷数据，节约 90% 的存储空间。
3. **连续聚合 (Continuous Aggregates)**：针对高频的流量大屏查询，创建了 	raffic_5min 连续聚合视图，配置后台按分钟自动刷新。大屏查询不再扫描原表，直接命中视图返回结果。
4. **数据生命周期 (Retention)**：部署了 30 天的保留策略 (dd_retention_policy)，自动淘汰超期明细数据，适配边缘硬件有限的磁盘容量。
