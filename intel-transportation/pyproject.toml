[project]
name = "intel-transportation"
version = "0.1.0"
description = "Intelligent transportation detection, LPR, event, prediction, and dashboard pipeline"
requires-python = ">=3.10,<3.14"

[tool.ruff]
target-version = "py310"
line-length = 120
extend-exclude = [
  ".venv",
  ".git",
  ".idea",
  "data",
  "models",
  "tmp",
  "backend/prediction/models",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "B"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["acceptance", "backend", "core", "db", "edge"]

[tool.ruff.lint.flake8-bugbear]
# FastAPI 的依赖声明本身就写在参数默认值里，B008 在这里是误报
extend-immutable-calls = ["fastapi.Depends", "fastapi.File", "fastapi.Form"]

[tool.pytest.ini_options]
testpaths = ["tests", "backend/prediction/tests", "backend/crew/tests", "backend/flink/tests", "backend/vision/tests", "backend/integration/tests", "acceptance/tests"]
pythonpath = ["."]
addopts = "-ra --strict-markers"

[tool.coverage.run]
branch = true
source = ["core", "backend", "db", "edge"]
omit = ["*/tests/*", "*/__pycache__/*"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_empty = true
precision = 2
